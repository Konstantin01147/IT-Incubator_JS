function arrayDiff(a, b) {

    for (var i = 0; i < a.length; i++) {

        if (a[i] == b[0]) {
            a.delete[i];
            i = 0;

        }
        console.log(a[i]);
    }
    return a;
}
